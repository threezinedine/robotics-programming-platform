{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Libraries",
            "type": "shell",
{% if PLATFORM == "Windows" %}
            "command": "python",
            "args": [
                "${workspaceFolder}/../config.py",
                "-p",
                "libraries",
                "build"
            ],
{% else %}
            "command": "sudo",
            "args": [
                "python3",
                "${workspaceFolder}/../config.py",
                "-p",
                "libraries",
                "build"
            ],
{% endif %}
            "options": {
                "cwd": "${workspaceFolder}/.."
            }
        }
    ]
}