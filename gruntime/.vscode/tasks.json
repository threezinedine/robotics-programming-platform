{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build graphical runtime",
            "type": "shell",
            "command": "sudo", // TODO: Change to python if needed using .in file
            "args": [
                "python3", // TODO: Change to python if needed using .in file
                "${workspaceFolder}/../config.py",
                "-p",
                "gruntime",
                "build"
            ],
            "options": {
                "cwd": "${workspaceFolder}/.."
            }
        }
    ]
}